@model NewsApplication.Models.User


@{ 
    ViewBag.Title = "Thong tin da nhap";
}

ID: @Model.id  + @Model.username + @Model.password  + @Model.phone + @Model.role + @Model.email

PHONE FROM DaTA @ViewBag.phone