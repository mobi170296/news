<div id="header">
    <div id="header-logo">
        <div id="header-info" class="u-fr clearfix">
            <div>Hotline: 0795759696</div>
            @if (ViewBag.user.IsLogin())
            {
                <div>
                    Xin chào <a href="/User">@ViewBag.user.lastname @ViewBag.user.firstname!</a> @Html.ActionLink("Đăng xuất", "Logout", "User")
                </div>
                if (ViewBag.user.HaveRole(NewsApplication.Models.User.ADMIN))
                {
                    <div>
                        @Html.ActionLink("Quản trị hệ thống", "Index", "Admin")
                    </div>
                }

                if (ViewBag.user.HaveRole(NewsApplication.Models.User.JOURNALIST))
                {
                    <div>
                        @Html.ActionLink("Quản lý bài báo", "Index", "Posts")
                    </div>
                }
                if (ViewBag.user.HaveRole(NewsApplication.Models.User.INSPECTOR))
                {
                    <div>
                        @Html.ActionLink("Rà soát bài đăng", "Index", "Inspector")
                    </div>
                }
            }
            else
            {
                <div>@Html.ActionLink("Đăng nhập", "Login", "User") | @Html.ActionLink("Đăng ký", "Register", "User")</div>
            }


        </div>
        <a id="header-logo-img" href="/"></a>
    </div>
    <div id="main-menu">
        <div id="main-menu-wrapper" class="clearfix">
            <div class="main-menu-item"><a href="#">TRONG NƯỚC</a></div>
            <div class="main-menu-item"><a href="#">QUỐC TẾ</a></div>
            <div class="main-menu-item"><a href="#">BẠN ĐỌC</a></div>
            <div class="main-menu-item"><a href="#">CÔNG ĐOÀN</a></div>
            <div class="main-menu-item"><a href="#">KINH TẾ</a></div>
            <div class="main-menu-item"><a href="#">SỨC KHỎE</a></div>
            <div class="main-menu-item"><a href="#">GIÁO DỤC</a></div>
            <div class="main-menu-item"><a href="#">PHÁP LUẬT</a></div>
            <div class="main-menu-item"><a href="#">VĂN NGHỆ</a></div>
            <div class="main-menu-item"><a href="#">THỂ THAO</a></div>
            <div class="main-menu-item"><a href="#">CÔNG NGHỆ</a></div>
            <div class="main-menu-item"><a href="#">THỊ TRƯỜNG</a></div>
            <div class="main-menu-item"><a href="#">PHỤ NỮ</a></div>
            <div class="main-menu-item"><a href="#">ĐỊA PHƯƠNG</a></div>
        </div>
    </div>
</div>